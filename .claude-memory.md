# 项目上下文 - 电瓶车租赁管理系统

## 项目路径
`E:\rent`

## 技术栈
- 微信小程序 + 云开发
- Vant WeApp UI
- ECharts 图表

## 数据库集合
- `car` - 车辆表（含 create_by）
- `rental` - 订单表（含 create_by）
- `user` - 用户表
- `operation_log` - 操作日志表

## 项目分支
`superpowers` - 有未提交的修改

---

## 当前进度

### 已完成改造

1. **静默登录** - openId 自动获取，存入 globalData.openId
2. **用户体系** - user 表存储用户信息（名字、头像）
3. **权限隔离** - create_by 字段隔离数据
4. **操作日志** - 每次增删改记录到 operation_log
5. **车辆状态流转**：
   - 0（空闲）→ 商家手动"租出"按钮
   - 1（租聘中）→ checkExpiredRentals 云函数检测过期自动变更为 2（待结算）
   - 2（待结算）→ 结算后自动变更为 0（空闲）

### 首页功能按钮
- 🔑 租聘 (carStatus=0) → 车库只显示空闲车辆
- 🔄 续租 (carStatus=1) → 车库只显示租聘中车辆
- 🧾 结算 (carStatus=2) → 车库只显示待结算车辆

### 核心页面流程
```
首页 → 租聘/续租/结算 → 车库(选择模式) → 对应操作页面
```

### 车库页面（garage）
- **选择模式**：首页点击租聘/续租/结算后进入，带 carStatus 参数筛选
- **非选择模式**：点击"我的车库"进入，显示全部车辆和标签页
- 标签页可切换（全部/空闲/租聘中/待结算），非选择模式下正常过滤
- 选择模式下整个卡片可点击，提示文案为"点击卡片选择要租聘的车辆"

### 历史订单页面（order-history）
- 日期字段（startTime/expireTime）已格式化为字符串显示
- 已按 create_by 隔离数据

---

## 近期修复

### 2026-04-13
- 车库页面选择模式：点击租聘/续租/结算后进入车库，整个卡片可点击选择
- 车库页面标签切换：非选择模式下切换标签页可正确过滤车辆
- 历史订单日期显示：startTime/expireTime 从 object 格式化为 YYYY-MM-DD 字符串

---

## 待处理事项

### TODO 列表
- [ ] 商家报表导出功能
- [ ] 数据库权限规则配置（防止抓包绕过）
- [ ] 价格/押金体系设计
- [ ] 逾期费用计算规则

### 代码问题
- stats.js 统计页面部分数据计算逻辑可优化

---

## 关键文件路径

### 核心页面
- `miniprogram/pages/home/home.js` - 首页
- `miniprogram/pages/profile/profile.js` - 个人中心
- `miniprogram/subPackages/car/pages/garage/garage.js` - 车库
- `miniprogram/subPackages/order/pages/rental/rental.js` - 租车
- `miniprogram/subPackages/order/pages/renew/renew.js` - 续租
- `miniprogram/subPackages/order/pages/settle/settle.js` - 结算

### 公共文件
- `miniprogram/app.js` - 全局数据（openId、userInfo、addOperationLog）

---

## 代码规范

| 类型 | 规则 |
|------|------|
| 普通变量 | camelCase |
| 常量 | UPPER_SNAKE_CASE |
| 函数名 | camelCase + 动词前缀 |
| CSS 类名 | kebab-case |
| 文件名 | kebab-case |
